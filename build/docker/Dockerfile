# Nginx
#
# VERSION               0.0.1

FROM      buildpack-deps:16.04-scm
LABEL Description="Buildenvironment" Vendor="wetgenes" Version="0.0"
RUN apt-get update && apt-get install -y sudo && ./apt-gets && ./install

