generic data that is usuall coppied into other projects when baked

This is mostly just a place to store master copies of datafiles so
it often needs to available when baking apps but never when running them

