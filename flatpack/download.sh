
#TODO

echo need to parse this, extract the url for multiple .snap files unpack them using squashfs and then we have a multiple binary builds we can publish in a flatpack

curl -H 'Snap-Device-Series: 16' http://api.snapcraft.io/v2/snaps/info/gamecake

